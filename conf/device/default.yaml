# =============================================================================
# Device 配置
# =============================================================================

accelerator: gpu          # 加速器: "gpu" | "cpu" | "auto"
precision: bf16-mixed     # 精度: "bf16-mixed" | "16-mixed" | "32"
